// var user = ["cheenu" , "admin" , 5];
// var [name,role,courseCount] = user;
// console.log(courseCount);

const myUser = {
    name : "cheenu",
    courseCount : 10,
    role : "admin",
    rating : 5
};

const {name,courseCount,role,rating} = myUser;
console.log(rating);
